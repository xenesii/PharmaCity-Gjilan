import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryBlue = Color(0xFF0F4C81); // Royal Blue from your logo
  static const Color accentTeal = Color(0xFF14B1AB);  // Teal accent
  static const Color darkBlue = Color(0xFF08306B);   // Darker blue for headers
  static const Color lightBackground = Color(0xFFF5F5F5);
  static const Color cardLight = Color(0xFFF2EBE9);   // Beige/light card background
  static const Color textDark = Color(0xFF1A1A1A);
  static const Color buyButtonBlue = Color(0xFF0F4C81); // Dark blue for buy buttons
  static const Color cartIconBlue = Color(0xFF0F4C81);
}